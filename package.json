{
  "scripts": {
    "dev": "next dev",
    "client:dev": "next dev --prefix client",
    "server:dev": "nest start --watch --prefix server",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "cloudinary-build-url": "^0.2.4"
  }
}
